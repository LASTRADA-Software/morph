// SPDX-License-Identifier: Apache-2.0
//
// An ordinary file with nothing to say about bidi at all. The valid set must
// contain one, or "accepted" would only ever mean "accepted a file that
// mentions the subject".

import QtQuick

Item {
    property string sign: "+"
}
